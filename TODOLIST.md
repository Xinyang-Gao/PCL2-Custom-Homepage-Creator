# 待办清单

> 根据当前代码实现情况整理的优先级任务。  
> 编号规则：P0（严重问题）> P1（高优先级）> P2（中优先级）> P3（低优先级）> P4（可选）。  
> **状态说明**：✅ 已完成 | ⚠️ 部分完成 | ❌ 未完成

---

## 🔴 P0 – 严重问题

| 编号 | 问题 | 说明 | 解决方案/状态 |
| ---- | ---- | ---- | ---- |
| #A1  | 事件绑定功能缺失（预览无实际响应） | 编辑器内可设置 `EventType` 和 `EventData`，但预览时按钮点击无任何效果。 | ❌ 未完成：需在 `RenderManager` 中为可点击组件添加 `click` 监听，根据 `EventType` 执行相应动作。 |

---

## 🟠 P1 – 高优先级（功能缺陷或重大体验）

| 编号 | 任务 | 说明 | 解决方案/状态 |
| ---- | ---- | ---- | ---- |

---

## 🟡 P2 – 中优先级（体验优化）

| 编号 | 任务 | 说明 | 解决方案/状态 |
| ---- | ---- | ---- | ---- |
| #C4  | 移动端触屏拖拽 | 手机/平板上无法拖拽组件。 | ❌ 未实现 |
| #C6  | 导出 XAML 时的格式选项 | 缩进固定 2 空格，用户可能希望自定义。 | ❌ 未实现 |

---

## 🔵 P3 – 低优先级（性能、代码质量、边缘情况）

| 编号 | 任务 | 说明 | 解决方案/状态 |
| ---- | ---- | ---- | ---- |
| #D1  | 组件库分组和收藏 | 将组件分为“基础控件”“布局容器”“交互组件”等分组，支持折叠/展开。 | ❌ 未实现 |
| #D2  | 导出为图片（截图） | 生成设计稿的 PNG 图片。 | ❌ 未实现 |
| #D3  | 撤销/重做增加栈可视化 | 在界面某处显示历史栈深度，支持跳转到任意历史状态。 | ❌ 未实现 |

---

## ⚪ P4 – 锦上添花

| 编号 | 任务 | 说明 |
| ---- | ---- | ---- |
| #E1  | 支持组件模板（保存为模板） | 将常用组件组合保存为模板，拖拽即用。 |

---

## 新发现的问题（待修复）

| 编号 | 问题 | 描述 | 影响 | 建议修复 |
| ---- | ---- | ---- | ---- | ---- |
| #F1  | Margin 滑块实时调整未记录历史 | `renderManager.js` 中 `updateComponentMargin` 直接修改属性并刷新样式，但不触发历史记录。 | 用户调整边距后无法撤销/重做。 | ❌ 未完成：在滑块 `mouseup` 事件中调用 `applyCurrentProps` 统一提交变更；或记录单独的 `UPDATE_PROPS` 操作。 |
| #F2  | 历史操作中移动组件时索引可能漂移 | 撤销移动操作时依赖记录的 `oldIndex`，但若移动后其他组件增减，索引可能无效。 | 理论上撤销可能错位，但因每次移动后立即记录，实际发生概率低。 | ❌ 未完成：在 `doMove` 中根据组件 ID 重新查找当前位置，而不是依赖索引。 |