## 仍存在的问题（需继续修复）

| 类别 | 问题 | 状态 | 说明 |
|------|------|------|------|
| 健壮性 | 关闭页面无未保存提示 | ❌ 未修复 | 未监听 `beforeunload` 事件，用户可能意外丢失更改 |
| 健壮性 | 组件复制 ID 基准优化 | ⚠️ 可选 | `deepCloneComponent` 已调用 `getMaxGlobalId()` 确保 ID 不冲突，但每次克隆都遍历全树，性能可接受；可考虑维护全局最大 ID 变量 |
| 性能 | 超大组件树（>200）渲染 | 🔵 待优化 | `renderCanvas` 全量重建 DOM 可能导致卡顿，可考虑虚拟滚动或增量更新 |
| 功能 | Grid 布局的附加属性（如 `Grid.Row`）在 UI 中不可见 | 🔵 低优先级 | 这些属性仅在 XAML 生成时保留，渲染时忽略，用户无法在属性面板直接编辑（依赖自定义属性） |

## 更新后的 TODO List

### 🟠 高优先级（P1 – 用户体验/数据安全）
- **关闭页面未保存提示**  
  - 文件：`script.js` 初始化  
  - 计划：监听 `beforeunload`，若 `App.state.dirty === true` 返回标准提示字符串。  
  - 预计工时：0.5h

### 🟡 中优先级（P2 – 健壮性与性能） 
- **组件复制 ID 基准优化（可选）**  
  - 当前实现已足够安全，但可缓存全局最大 ID 避免重复遍历。  
  - 预计工时：0.5h

### 🔵 低优先级（P3 – 体验增强）
- **超大组件树渲染性能**  
  - 问题：当组件数量超过 200 时，`renderCanvas` 全量重建可能卡顿。  
  - 计划：引入虚拟滚动或仅更新变化区域（使用 `refreshComponent` 单个更新，但添加/删除仍需全量渲染）。  
  - 预计工时：2h  
- **Grid 附加属性可视化**  
  - 问题：`Grid.Row`、`Grid.Column` 等仅在 XAML 中保留，属性面板无法直接编辑。  
  - 计划：在特殊属性分组中增加“Grid 布局”区域，提供数值输入。  
  - 预计工时：1h  

---

## 总结
当前项目核心功能完整，已解决大部分记录的问题。剩余主要风险为 **关闭页面未保存提示** 和 **超大组件树的渲染性能**。